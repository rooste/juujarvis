spring:
  application:
    name: juujarvis

server:
  port: 8080

juujarvis:
  anthropic:
    api-key: ${ANTHROPIC_API_KEY:}
    model: claude-sonnet-4-20250514

logging:
  level:
    com.juujarvis: DEBUG
